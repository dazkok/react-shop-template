import React from 'react';

const GlobalNotify = () => {
    return (
        <div className={'container-fluid bg-black d-flex align-items-center justify-content-center global-notify'}
             style={{height: '40px'}}>
            FREE DELIVERY ON ORDERS OVER 200 ZŁ
        </div>
    );
};

export default GlobalNotify;
